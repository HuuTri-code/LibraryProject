#include "Sach.h"
using namespace std;

int timViTriChen(ListDauSach ds, const char* tenSach) {
    int left = 0;
    int right = ds.n - 1;

    while (left <= right) {
        int mid = left + ()
    }
}